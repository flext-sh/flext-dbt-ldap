# SonarCloud automatic analysis configuration (GitHub App mode).
# text:S8565 (lock file missing) is released only on pyproject.toml: its remedy
# is forbidden by the fleet lockfile ban (operator decision 2026-09-23, bead
# flext-c8yle). Every other rule keeps analyzing pyproject.toml.
sonar.issue.ignore.multicriteria=lockfile
sonar.issue.ignore.multicriteria.lockfile.ruleKey=text:S8565
sonar.issue.ignore.multicriteria.lockfile.resourceKey=**/pyproject.toml
